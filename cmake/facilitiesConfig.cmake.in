get_filename_component(facilities_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

if(NOT TARGET ST::facilities)
    include("${facilities_CMAKE_DIR}/facilitiesTargets.cmake")
endif()

set(facilities_LIBRARIES ST::facilities)
